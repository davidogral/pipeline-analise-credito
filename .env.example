# Copie para .env e ajuste se necessário. Os mesmos valores são lidos pelo pipeline e pelo docker compose.
PGHOST=localhost
PGPORT=5432
PGDATABASE=pipeline
PGUSER=postgres
PGPASSWORD=postgres
# Opcional: outro diretório para o data lake local (padrão: ./data)
# PIPELINE_DATA_DIR=./data
